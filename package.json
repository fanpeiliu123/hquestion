{
  "name": "hq-platform",
  "version": "1.0.0",
  "description": "HQ (海问) - AI-Powered Self-Service Consultation Platform for Chinese AI Enterprise Globalization & ESG Innovation",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "seed": "cd server && node seed.js",
    "setup": "npm install && cd server && npm install && cd ../client && npm install && cd .. && npm run seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
